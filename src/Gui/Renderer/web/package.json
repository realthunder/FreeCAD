{
  "name": "fcviewer-web",
  "private": true,
  "version": "0.1.0",
  "description": "DOM UI layer of the FreeCAD thin client (docs/ThinClient.md): SolidJS chrome over the WASM viewer canvas",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.0"
  },
  "dependencies": {
    "solid-js": "^1.9.0"
  }
}
