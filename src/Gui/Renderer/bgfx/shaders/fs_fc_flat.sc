$input v_color0

/*
 * FreeCAD flat fragment shader for lines and points, unclipped variant
 * (no discard, keeps early-Z); the body lives in fc_flat_fs.sh.
 */

#include <bgfx_shader.sh>
#include "fc_flat_fs.sh"
